---
# @meta description: >
# Role to install the OpenSearch Operator on a Kubernetes cluster.
# It installs the OpenSearch Operator via its helm chart.
# @end
galaxy_info:
  description: Install OpenSearch Operator
  role_name: opensearch_operator
  author: Clément Hubert

  license: MPL-2.0

  min_ansible_version: "2.17"

  galaxy_tags:
    - kubernetes
    - opensearch
    - operator

dependencies: []
