---
- name: Verify
  hosts: localhost
  connection: local
  gather_facts: true

  tasks:
    - name: "Pause for cleanup to complete."
      ansible.builtin.pause:
        seconds: 30

    - name: "Check if 'redis' namespace exists."
      kubernetes.core.k8s_info:
        kind: Namespace
        name: "redis"
      register: namespace_redis
      failed_when: false
      changed_when: false

    - name: Assert that 'redis' namespace does not exist
      ansible.builtin.assert:
        that: namespace_redis['resources'] | length == 0
