---
- name: "Include setup when enabled"
  ansible.builtin.include_tasks:
    file: "setup.yml"
    apply:
      tags:
        - "mongodb_operator"
        - "install"
  when: mongodb_operator_enabled
  tags:
    - "mongodb_operator"
    - "install"

- name: "Include cleanup when disabled"
  ansible.builtin.include_tasks:
    file: "cleanup.yml"
    apply:
      tags:
        - "mongodb_operator"
        - "uninstall"
  when: not mongodb_operator_enabled
  tags:
    - "mongodb_operator"
    - "uninstall"
