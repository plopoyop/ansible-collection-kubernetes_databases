---
- name: Verify
  hosts: localhost
  connection: local
  gather_facts: true

  tasks:
    - name: "Wait for 'postgres-instance' namespace to be removed."
      kubernetes.core.k8s_info:
        kind: Namespace
        name: "postgres-instance"
      register: namespace_postgres
      until: namespace_postgres.resources | length == 0
      retries: 30
      delay: 5
      changed_when: false

    - name: "Assert that 'postgres-instance' namespace is removed."
      ansible.builtin.assert:
        that: namespace_postgres.resources | length == 0
        fail_msg: "Namespace 'postgres-instance' is not removed."
