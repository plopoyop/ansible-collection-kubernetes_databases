---
- name: Verify
  hosts: localhost
  connection: local
  gather_facts: true

  tasks:
    - name: "Pause for job to complete."
      ansible.builtin.pause:
        seconds: 30

    - name: "Check if 'mariadb-operator' namespace exists."
      kubernetes.core.k8s_info:
        kind: Namespace
        name: "mariadb-operator"
      register: namespace_mariadb_operator
      failed_when: false
      changed_when: false

    - name: Assert that 'mariadb-operator' namespace does not exist
      ansible.builtin.assert:
        that: namespace_mariadb_operator['resources'] | length == 0
